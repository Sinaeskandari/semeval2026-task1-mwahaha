# Core deep-learning stack
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.24.0
bitsandbytes>=0.41.0
peft>=0.7.0
trl>=0.7.0
xformers>=0.0.22

# Fast 4-bit loading / fine-tuning
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Retrieval-Augmented Generation
datasets>=2.14.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0

# Data handling & utilities
pandas>=2.0.0
tqdm>=4.65.0

# Synthetic-data generation (teacher model, notebooks only)
google-generativeai>=0.5.0
